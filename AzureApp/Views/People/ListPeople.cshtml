@model IEnumerable<AzureApp.Models.PersonModel>

@{
    ViewData["Title"] = "ListPeople";
}

<h1>ListPeople</h1>
<input id="nameInput" value="ABC" />
<button id="addButton" class="alert-danger">Add</button>
<button value="Hejsan">Go!</button>
<p id="nameOut">a</p>

@*<button value="Hejsan" onclick="location.href='@Url.Action("ListPeople", "People", new { name = "Haloo" })'">Go!</button>*@
<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                    @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                    @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
                </td>
            </tr>
        }
    </tbody>
</table>
